import { makeEnum, makeMaskEnum } from 'libs/enum';

describe('makeEnum', function() {
	it('fails test', function () {
		expect(true).to.be(false);
	});
});

describe('makeMaskEnum', function() {
	it('fails test', function () {
		expect(true).to.be(false);
	});
});